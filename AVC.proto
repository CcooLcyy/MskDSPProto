syntax = "proto3";

package AVCProto;

message Empty {}

// AVC 模块对外 gRPC 服务（占位）：用于上位机探测模块是否可达。
// TODO：后续在此 service 扩展 AVC 配置/运行态等接口。
service AVCService {
  // 健康探测：返回 OK 表示服务可达。
  rpc Ping(Empty) returns (Empty);
}
