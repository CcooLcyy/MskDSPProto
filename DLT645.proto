syntax = "proto3";

package DLT645Proto;

message Empty {}

// DLT645 模块对外 gRPC 服务（占位）：用于上位机探测模块是否可达。
// TODO：后续在此 service 扩展 DLT645 链路/点表/运行态等配置接口。
service DLT645Service {
  // 健康探测：返回 OK 表示服务可达。
  rpc Ping(Empty) returns (Empty);
}
